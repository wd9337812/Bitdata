# v0.33.0 测试留档

日期：2026-07-31

## 专项测试

```text
tests/test_s0_conditional_direction_v1.py
tests/test_s0_conditional_direction_v2.py

5 passed
```

同时执行标签路径专项回归：

```text
tests/test_s0_conditional_direction_v1.py
tests/test_s0_exit_profiles.py

8 passed
```

## 全量回归

```text
.venv\Scripts\python.exe -m pytest -q

375 passed in 19.20s
```

## 前端构建

```text
cd frontend
npm run build

vite v8.1.0
452 modules transformed
built in 863ms
```

## 数据验证

- `net_pct_1m` 与旧 10 分钟 `net_pct` 相关系数为 `1.0`，确认不能作为独立标签。
- 新候选直接读取原始 1 分钟 K 线生成 3/5/10 和 10/30/60 分钟标签。
- 路径缺失的候选不进入训练。
- 同一分钟双触发采用止损优先。
- 模型训练、早停、校准、验证、最终测试时间互斥。

## 模型产物验证

- 短周期与趋势周期使用独立版本和目录。
- 模型保存为普通字典结构，可在独立 Python 进程中反序列化。
- 两个候选均为 `research_only_not_eligible`。
- 两个候选均设置 `affects_live_admission=false`。

## 实盘安全

- 未修改线上策略配置；
- 未部署 VPS；
- 未签发实盘许可证；
- 未手工下单；
- 未修改 5U 硬停止、交易所止盈止损或裸仓保护。
