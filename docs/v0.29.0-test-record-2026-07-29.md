# v0.29.0 / V5.2 测试记录

日期：2026-07-29

## 验证范围

- V5.2 配置默认值与旧版本迁移
- 横截面优先的归一化权重
- 突破结构准入与纯动量实盘禁入
- 同币、同向、同形态的 30 分钟独立行情段压缩
- 12% / 18% / 24% / 30% 质量风险分档
- 5U 硬停止余量与 0.15U 额外预留
- 连续许可证和本地事故保护的 V5.2 能力继承
- 前端生产构建、首页、状态接口及全部拆分静态资源

## 自动化结果

- 后端全量测试：`358 passed`
- V5.2 专项测试：`56 passed`
- 前端生产构建：通过，`452 modules transformed`
- 本地 HTTP 冒烟：
  - `/`：HTTP 200
  - `/api/status`：HTTP 200
  - 生产构建生成的 4 个 JavaScript 资源和 1 个 CSS 资源：全部 HTTP 200

## 历史复盘口径

本次策略假设来自 VPS 全量历史的扣费后复盘，不把短期 PF 或重复影子机会直接当成优势：

- 817 笔历史实盘：毛收益约 `+24.18U`，手续费约 `69.69U`，资金费约 `-3.28U`，净收益约 `-48.79U`。
- 亏损后 5 分钟内同币同向再入场 88 笔：胜率约 `20.45%`，净收益约 `-33.09U`，PF 约 `0.41`。
- 当前版本影子按 30 分钟独立行情段压缩后，突破/预突破且横截面强度不低于约 `0.787` 的回顾样本为 31 个独立行情段，净收益约 `+3.00U`，PF 约 `3.41`。

最后一项属于历史回顾性证据，不是样本外收益承诺。V5.2 上线后必须使用独立版本数据继续验证，旧版本数据不参与其准入和仓位。

## 安全结论

- 不删除或绕过 5U 权益硬停止。
- 每个真实持仓仍必须同时具备 Binance 交易所端止损和止盈。
- 保留裸仓自动补保护、最小下单量、流动性硬门、API 限频和时间同步。
- MoE 继续只作研究影子，不接管 V5.2 实盘。
- 部署前后必须再次核查真实持仓、普通订单和条件保护单。
