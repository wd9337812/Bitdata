# v0.30.1 测试记录（2026-08-02）

## 范围

- 新增 `market_tsmom_consensus@s0_market_tsmom_28_56_v1` 独立研究影子。
- 新增每日市场指数构造、28/56 日趋势共振、BTC 多头影子、10% 止损和 120 小时退出。
- 新增无固定止盈的明确影子执行语义。
- Dashboard 新增中文研究卡片和专家设置。
- 线上实盘策略保持 `extreme_v5_roll@v5.2`，候选证据不参与实盘控制。

## 验证

- 定向隔离与影子执行：`37 passed`。
- 完整后端：`644 passed`。
- 前端生产构建：通过，生成版本化静态资源 `index-CNuHLSA_.js`。
- `git diff --check`：通过，仅有 Windows LF/CRLF 提示。
- 既有 `test_s0_ctrend` 出现 5 条 ElasticNet 收敛警告，不是本次变更引入，测试仍通过。

## 数据与磁盘

- 研究结果写入 `data/research`，不纳入 Git 提交。
- VPS 审计时磁盘约 29GB、已用 11GB、可用 18GB、使用率 38%。
- 未删除活跃数据库、模型、实盘记录、影子记录或研究原始数据。
- 既有每日维护继续只压缩非活跃快照和清理可重建缓存，不执行会损失策略精度的数据删除。

## 发布边界

本次允许部署研究影子和 Dashboard，但不允许把候选切为实盘。只有未来独立影子样本在扣费后净收益、PF、回撤、市场状态覆盖和实时执行一致性上同时达标，才重新评估实盘接管。

