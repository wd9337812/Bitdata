# S0 Bug / 性能 / API 频控审计（2026-08-04）

## Bug 审计

- 全量后端回归：`699 passed`（仅 5 条既有 ElasticNet 收敛警告）。
- 前端生产构建：452 模块通过；Dashboard 首页与新版 JS 资源 `index-BTbtmjer.js` 均 HTTP 200。
- 状态一致性：`runtime_protection_positions` 与交易所实际持仓一致；AKE 止损（0.0039052）与 3.5R 止盈（0.0063019）条件单完好；`last_error=''`。
- 24h 日志：runner 与 dashboard 均 0 条 429/418/500/502/Traceback/数据库锁；容器重启次数 0。

## 性能审计

- VPS：负载 1.09（1 核），内存 3.9G 总量、可用 2.9G；磁盘 40%（剩余 18G）。
- runner：CPU 33.8%，内存 112MiB；dashboard：CPU 0.2%，内存 258MiB。
- runner 心跳新鲜度：0.19 秒，循环正常。
- SQLite：主库 2718MB、WAL 235MB、可回收 1222MB（由维护定时器处理，不手动 VACUUM）。

## API 频控审计

- 当前分钟请求权重 62/2400（交易所限制 2.58%）；后台预算使用 4.7%。
- 订单计数：1 分钟内 2 单、10 秒内 1 单，远低于 1200/300 限制。
- 无冷却、无 429/418；`binance_rate.last_error=''`。
- 事件通道尚未启用，未新增 REST 压力；后续若接入事件扫描，需复用现有后台预算并保持保护/下单优先。

## 结论

当前 20% 固定档 + v0.31.7 前端在 Bug、性能、API 频控三项均通过审计；AKE 持仓保护完好，机器人正常运行。
