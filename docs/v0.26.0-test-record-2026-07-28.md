# v0.26.0 / V5.0-S30 测试记录

## 策略范围

- 当前 S0 实盘策略为 `extreme_v5_roll@v5.0-s30`。
- V5 使用独立的全仓短打准入、动态 `3x-10x` 杠杆、90% 计划保证金、12%-30% 风险区间和快速止盈止损。
- 旧 V3/V4 证据只保留用于历史回放，不参与 V5 的准入、信用或仓位。
- 普通亏损只降低下一次仓位；三连止损进入 20 分钟冷却，结束后自动回到 12% 风险基线。5U 硬停止、交易所保护单、最小下单量、API 限频和时间同步不变。

## 验证结果

- `python -m compileall -q app tests`：通过。
- `python -m pytest -q`：`339 passed`，仅有一个既有 `requests` 依赖兼容性 warning。
- `npm run build`：通过，Vite 生产资源构建成功。
- `git diff --check`：通过；仅有 Windows 换行提示。
- 本地配置冒烟：版本 `v5.0-s30`，策略族 `extreme_v5_roll`，S0 风险上限 `30%`，V5 风险基线 `12%`，保证金计划值 `90%`，杠杆范围 `3x-10x`。

## 部署边界

部署前后必须只读核验 VPS 的真实权益、持仓、普通订单和条件保护单；本文件只记录本地开发和测试结果，不代表线上部署已经完成。
