# v0.30.2 测试记录

## 范围

- 28/56 日市场趋势共振 V2 影子。
- 冻结样本外、扩展滚动前推、参数邻域、延迟、成本和季度分块抽样。
- 默认关闭的 S0 实盘接管决策。
- 日线 ATR 止损与通用分钟级保护隔离。
- 20 天最长持仓和信号关闭退出。
- 中文 Dashboard 和专家配置说明。

## 验证要求

- 专项后端测试。
- 全量后端测试。
- Python 编译检查。
- 前端生产构建。
- Dashboard 静态资源冒烟。
- 部署前后真实持仓、普通订单、条件止盈止损核查。
- VPS 版本、机器人、WebSocket、REST、时间偏差、磁盘和维护定时器核查。

## 发布约束

本版本部署后仍保持 `market_tsmom_live_enabled=false`，不会自动替换 V5.2。只有账户权益达到最低门槛、用户完成资金划转并再次确认后，才允许单独启用接管开关。启用时必须空仓，且先后核查交易所保护单。

## 本地结果

- 后端全量：`648 passed`，另有 5 条既有 ElasticNet 收敛告警，没有测试失败。
- 专项接管与保护：`22 passed`。
- Python `compileall`：通过。
- 前端 `vite build`：通过，共转换 452 个模块。
- 静态资源冒烟：`index.html` 引用 5 个资源，缺失 0 个。
- `git diff --check`：通过。

## VPS 部署验收

- 提交：`a12c966`。
- API 版本：`0.30.2`。
- Dashboard、入口脚本和 OpenAPI：HTTP 200。
- 机器人：运行中；路由：S0；当前实盘排序仍为 `v5.2`。
- 真实账户：权益约 `5.168U`，空仓，普通订单 0，条件订单 0。
- 趋势实盘接管：`false`；V2 只运行独立影子。
- 公共 WebSocket：已连接；私有 WebSocket：已初始化并实时连接。
- NTP：同步；部署后近两分钟 429/500/502/数据库锁/异常计数为 0。
- 磁盘：约 38% 已用；维护定时器 active。未删除活动数据库、交易历史、模型或研究数据。
